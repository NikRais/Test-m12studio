# Тестовое задание — верстка блока по макету

**Цель:** сверстать адаптивный блок с текстом и встроенными изображениями по методологии **БЭМ**, без использования JS и сторонних библиотек.

---

## 🧩 Технологии и подход

- **HTML5 + CSS3** — без JS.
- **Методология БЭМ**:  
  - Блок — `.article`  
  - Элементы — `__text`, `__img`, `__actions`  
  - Отдельный блок кнопки — `.button`
- **Семантика:** `<article>`, `<p>`, `<button>`, без лишних обёрток.  
- **Кроссбраузерность:** все свойства проверены на [caniuse.com](https://caniuse.com/).  
- **Валидация HTML:** пройдено через [W3C Validator](https://validator.w3.org/#validate_by_input) — ошибок нет.  
- **Адаптивность:** текст и изображения масштабируются синхронно при уменьшении ширины окна.

---

## 🧠 Особенности реализации

- Высота изображений рассчитывается через `em` → **совпадает с высотой текста** при любом размере.  
- Размеры, отступы и шрифты адаптируются плавно через `clamp()`.

---

## 🧾 Структура проекта

